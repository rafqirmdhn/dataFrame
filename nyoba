CREATE TABLE customer(
	id_customer VARCHAR(20) NOT NULL, 
	username VARCHAR(20) NOT NULL, 
	password VARCHAR(20) NOT NULL, 
	first_name VARCHAR(20) NOT NULL, 
	last_name VARCHAR(20) NOT NULL,
	email VARCHAR(20) NOT NULL, 
	gender CHAR(1) NOT NULl, 
	no_telp VARCHAR(15) NOT NULL, 
	alamat VARCHAR(50) NOT NULL 
	); 

CREATE TABLE admin(
	id_admin INT NOT NULL, 
	nama_admin VARCHAR(10) NOT NULL
	)

CREATE TABLE tiket{
	kode_tiket varchar(50) NOT NULL PRIMARY KEY,
	no_penerbangan varchar(50) NOT NULL,
	kelas varchar(100) NOT NULL,
	asal varchar(100) NOT NULL,
	tujuan varchar(100) NOT NULL,
	harga int(100) NOT NULL,
	kode_pesawat varchar(50) NOT NULL,
	FOREIGN KEY(kode_pesawat) REFERENCES pesawat(varchar)
};

CREATE TABLE customer_memesan_tiket(
	id_customer varchar(20) NOT NULL,
	kode_tiket varchar(20) NOT NULL,
	kode_pesan varchar(20) NOT NULL,
	nama_pemesan varchar(100) NOT NULL,
	no_identitas varchar(100) NOT NULL,
	tgl_penerbangan varchar(100) NOT NULL,
	waktu_terbang number(10) NOT NULL,
	tgl_lahir varchar(100) NOT NULL,
	FOREIGN KEY (id_customer) REFERENCES customer(id_customer) ON UPDATE CASCADE ON DELETE CASCADE,
	FOREIGN KEY (kode_tiket) REFERENCES tiket(kode_tiket) ON UPDATE CASCADE ON DELETE CASCADE
)

CREATE TABLE pesawat(
	kode_pesawat NUMBER PRIMARY KEY NOT NULL AUTO_INCREMENT,
	nama_pesawat VARCHAR(50) NOT NULL,
	jenis_pesawat VARCHAR(50) NOT NULL

);


CREATE TABLE maskapai(
	kode_maskapai NUMBER PRIMARY KEY NOT NULL AUTO_INCREMENT,
	nama_maskapai VARCHAR(50) NOT NULL

);


INSERT INTO pesawat(kode_pesawat, nama_pesawat, jenis_pesawat)
VALUES ('G001','Boeing',777');

INSERT INTO maskapai(kode_maskapai, nama_maskapai)
VALUES ('5001','Garuda');

##Insert Record

INSERT INTO customer VALUES(
	10001,
	"budi365",
	"12345678",
	"Budi",
	"Soetomo",
	"budi@gmail.com",
	"L",
	"8211648987",
	"Kopo"
	);

INSERT INTO admin VALUES(
	201,
	"Luther Hariyanto"
);

INSERT INTO tiket VALUES(
	30001,
	'GA-JB01',
	'firstclass',
	'Soekarno Hatta (Jakarta)',
	'Ngurah Rai (Bali)',
	30000000,
	4001
	);
